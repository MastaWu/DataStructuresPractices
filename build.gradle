apply plugin: 'java'

repositories {
    mavenCentral()
}

dependencies {
    testCompile 'junit:junit:4.12'
}

sourceSets {
    main {
        java {
            srcDir 'java'
        }
    }
}

task wrapper(type: Wrapper) {
    gradleVersion = '2.13' //version required
}